# 📚 Learning2ne1 Forum

Diễn đàn sinh viên được xây dựng bằng PHP thuần, cho phép người dùng chia sẻ bài viết, thảo luận, bình luận và quản lý tài liệu học tập.

![PHP](https://img.shields.io/badge/PHP-8.1+-blue.svg)
![MySQL](https://img.shields.io/badge/MySQL-5.7+-orange.svg)
![License](https://img.shields.io/badge/License-MIT-green.svg)

## 📋 Mục lục

-   [Giới thiệu](#giới-thiệu)
-   [Cấu trúc thư mục](#cấu-trúc-thư-mục)
-   [Yêu cầu hệ thống](#yêu-cầu-hệ-thống)
-   [Cài đặt](#cài-đặt)
-   [Cơ sở dữ liệu](#cơ-sở-dữ-liệu)
-   [Các chức năng chi tiết](#các-chức-năng-chi-tiết)
-   [Tác giả](#tác-giả)

---

## 🎯 Giới thiệu

**Learning2ne1 Forum** là một diễn đàn dành cho sinh viên với các tính năng:

-   Đăng ký/Đăng nhập tài khoản
-   Tạo, sửa, xóa bài viết với file đính kèm
-   Bình luận và trả lời bình luận (nested comments)
-   Like bài viết
-   Tìm kiếm và lọc bài viết
-   Xem bài viết Trending
-   Dashboard thống kê xu hướng (biểu đồ Chart.js)
-   Kho tài liệu học tập
-   Quản lý người dùng (Admin)

---

## 📁 Cấu trúc thư mục

```
Learning2ne1-Forum/
├── assets/
│   └── css/              # File CSS cho giao diện
├── database/
│   └── bikvyzpx_k69_nhom1.sql  # File SQL tạo database
├── includes/
│   ├── functions.php     # Các hàm xử lý chung
│   └── navbar.php        # Thanh navigation
├── pages/
│   ├── admin_users.php   # Quản lý người dùng (Admin)
│   ├── create_post.php   # Tạo bài viết mới
│   ├── dashboard.php     # Dashboard
│   ├── login.php         # Đăng nhập
│   ├── logout.php        # Đăng xuất
│   ├── post. php          # Chi tiết bài viết
│   ├── profile.php       # Trang cá nhân
│   ├── register.php      # Đăng ký
│   └── resources. php     # Kho tài liệu
├── uploads/              # Thư mục lưu file upload
├── Tài liệu/             # Tài liệu dự án
├── config. php            # Cấu hình database
├── index. php             # Trang chủ
└── . htaccess             # Cấu hình Apache
```

---

## 💻 Yêu cầu hệ thống

-   **PHP** >= 8.1
-   **MySQL** >= 5.7
-   **Apache** với mod_rewrite enabled
-   **Web Browser** hiện đại (Chrome, Firefox, Edge...)

---

## 🚀 Cài đặt

### Bước 1: Clone repository

```bash
git clone https://github.com/StuDuu12/Learning2ne1-Forum.git
cd Learning2ne1-Forum
```

### Bước 2: Tạo database

1. Mở phpMyAdmin hoặc MySQL client
2. Tạo database mới: `bikvyzpx_k69_nhom1`
3. Import file `database/bikvyzpx_k69_nhom1.sql`

### Bước 3: Cấu hình kết nối

Chỉnh sửa file `config.php`:

```php
define('DB_HOST', 'localhost');
define('DB_USER', 'root');
define('DB_PASS', '');
define('DB_NAME', 'bikvyzpx_k69_nhom1');
```

### Bước 4: Phân quyền thư mục upload

```bash
chmod 777 uploads/
```

### Bước 5: Truy cập

Mở trình duyệt và truy cập: `http://localhost/Learning2ne1-Forum/`

---

## 🗄️ Cơ sở dữ liệu

### Sơ đồ các bảng chính

| Bảng             | Mô tả                                                                     |
| ---------------- | ------------------------------------------------------------------------- |
| `user`           | Thông tin người dùng (id_user, ho_ten, username, password, account_level) |
| `posts`          | Bài viết (id, user_id, title, content, tags, privacy, status, views)      |
| `comments`       | Bình luận (id, post_id, user_id, content, parent_id)                      |
| `likes`          | Lượt thích (id, user_id, target_id, target_type)                          |
| `attachments`    | File đính kèm (id, post_id, file_path, file_type)                         |
| `tai_lieu`       | Tài liệu học tập (id_tai_lieu, id_user, tieu_de, mo_ta, duong_dan_file)   |
| `user_interests` | Sở thích người dùng theo tag (user_id, tag, score)                        |

### Phân quyền tài khoản (account_level)

-   `0` - Admin (Quản trị viên)
-   `1` - Giảng viên
-   `2` - Sinh viên

---

## 📝 Các chức năng chi tiết

---

### 1. 🔐 ĐĂNG KÝ TÀI KHOẢN

**File:** `pages/register.php`

#### Luồng xử lý:

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. HIỂN THỊ FORM ĐĂNG KÝ                                        │
│    └─> Form với các trường:  username, password, confirm_password│
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 2. NGƯỜI DÙNG SUBMIT FORM (POST)                                │
│    └─> Gửi dữ liệu:  username, password, confirm_password        │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 3. VALIDATE DỮ LIỆU                                             │
│    ├─> Kiểm tra username không rỗng và >= 3 ký tự              │
│    ├─> Kiểm tra password không rỗng và >= 6 ký tự              │
│    └─> Kiểm tra confirm_password khớp với password             │
│                                                                 │
│    [LỖI] ──> Lưu errors vào SESSION, redirect về register. php  │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 4. KIỂM TRA USERNAME TỒN TẠI                                    │
│    └─> Query:  SELECT id_user FROM user WHERE username = ?       │
│                                                                 │
│    [ĐÃ TỒN TẠI] ──> Báo lỗi "Username đã tồn tại"              │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 5. TẠO TÀI KHOẢN MỚI                                            │
│    ├─> Gán account_level = 2 (Sinh viên)                       │
│    ├─> ho_ten = username (mặc định)                            │
│    └─> INSERT INTO user (ho_ten, username, password,           │
│           account_level) VALUES (?, ?, ?, ?)                   │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 6. REDIRECT                                                     │
│    └─> Hiển thị "Đăng ký thành công!", chuyển đến login.php    │
└─────────────────────────────────────────────────────────────────┘
```

---

### 2. 🔑 ĐĂNG NHẬP

**File:** `pages/login.php`

#### Luồng xử lý:

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. HIỂN THỊ FORM ĐĂNG NHẬP                                      │
│    └─> Form với các trường: username, password                 │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 2. NGƯỜI DÙNG SUBMIT FORM (POST)                                │
│    └─> Gửi dữ liệu: username, password                         │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 3. VALIDATE DỮ LIỆU                                             │
│    ├─> Kiểm tra username không rỗng                            │
│    └─> Kiểm tra password không rỗng                            │
│                                                                 │
│    [LỖI] ──> Lưu errors vào SESSION, redirect về login.php    │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 4. TRUY VẤN USER TỪ DATABASE                                    │
│    └─> SELECT id_user, ho_ten, username, password,             │
│           account_level FROM user WHERE username = ?           │
│                                                                 │
│    [KHÔNG TÌM THẤY] ──> Báo lỗi "Username không tồn tại"       │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 5. XÁC THỰC MẬT KHẨU                                            │
│    └─> So sánh password nhập vào với password trong DB         │
│                                                                 │
│    [SAI] ──> Báo lỗi "Mật khẩu không chính xác"                │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 6. TẠO SESSION                                                  │
│    ├─> $_SESSION['user_id'] = user. id_user                     │
│    ├─> $_SESSION['username'] = user.username                   │
│    ├─> $_SESSION['role'] = user.account_level                  │
│    ├─> $_SESSION['account_level'] = user.account_level         │
│    └─> $_SESSION['user_fullname'] = user. ho_ten                │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 7. REDIRECT                                                     │
│    └─> Chuyển hướng về index.php                               │
└─────────────────────────────────────────────────────────────────┘
```

---

### 3. 🏠 TRANG CHỦ - HIỂN THỊ BÀI VIẾT

**File:** `index.php`

#### Luồng xử lý:

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. KHỞI TẠO                                                     │
│    ├─> Include:  config.php, functions.php                      │
│    ├─> Kiểm tra đăng nhập:  isLoggedIn()                        │
│    └─> Lấy current_user nếu đã đăng nhập                       │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 2. XỬ LÝ CÁC ACTION (POST REQUEST)                              │
│    └─> action = 'toggle_like': Like/Unlike bài viết            │
│        ├─> Kiểm tra đã like chưa (SELECT FROM likes)           │
│        ├─> Nếu đã like → DELETE FROM likes                     │
│        └─> Nếu chưa → INSERT INTO likes                        │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 3. LẤY DỮ LIỆU BÀI VIẾT                                         │
│    ├─> Lấy bài trending:  getTrending($pdo, 5)                  │
│    │   (Bài có >= 5 likes, >= 5 comments trong 7 ngày)         │
│    │                                                            │
│    └─> Lấy tất cả bài viết:                                     │
│        [Đã đăng nhập] → Lấy bài public + bài riêng tư của mình │
│        [Khách] → Chỉ lấy bài public                            │
│                                                                 │
│        SELECT p.*, u.ho_ten, u. username,                       │
│          (COUNT likes), (COUNT comments)                        │
│        FROM posts p JOIN user u                                │
│        ORDER BY created_at DESC LIMIT 100                      │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 4. LỌC VÀ TÌM KIẾM                                              │
│    ├─> Tìm kiếm theo từ khóa (? search=...)                     │
│    │   → Tìm trong:  title, content, tags, ho_ten               │
│    │                                                            │
│    ├─> Lọc theo trạng thái (?status=solved/unsolved)           │
│    │                                                            │
│    ├─> Lọc theo thời gian (? time=today/week/month)             │
│    │                                                            │
│    └─> Sắp xếp (? sort=newest/oldest/most_liked/most_commented) │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 5. HIỂN THỊ GIAO DIỆN                                           │
│    ├─> Navbar (include navbar.php)                             │
│    ├─> Search box + Filter bar                                 │
│    ├─> Tabs:  "Tất cả" | "Trending"                             │
│    └─> Grid các bài viết với:                                   │
│        ├─> Avatar + tên tác giả + thời gian                    │
│        ├─> Tiêu đề + nội dung rút gọn                          │
│        ├─> Tags                                                │
│        ├─> Preview ảnh/file đính kèm                           │
│        └─> Buttons: Like, Comment, Hide                        │
└─────────────────────────────────────────────────────────────────┘
```

---

### 4. 📊 DASHBOARD - THỐNG KÊ XU HƯỚNG

**File:** `pages/dashboard.php`

#### Luồng xử lý:

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. KIỂM TRA ĐĂNG NHẬP                                           │
│    └─> Nếu chưa đăng nhập → Redirect về login.php              │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 2. LẤY THỐNG KÊ CỘNG ĐỒNG                                       │
│    └─> getTagStats($pdo)                                       │
│        → Phân tích tags từ tất cả bài viết                     │
│        → Đếm số lần xuất hiện mỗi tag                          │
│        → ORDER BY count DESC                                   │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 3. LẤY THỐNG KÊ CÁ NHÂN                                         │
│    └─> getUserInterests($pdo, user_id)                         │
│        → SELECT tag, score FROM user_interests                 │
│        → Dựa trên bài viết user đã xem/tương tác               │
│        → ORDER BY score DESC                                   │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 4. THỐNG KÊ HỆ THỐNG (CHỈ ADMIN)                                │
│    ├─> Tổng số users:    SELECT COUNT(*) FROM user             │
│    ├─> Tổng số bài viết: SELECT COUNT(*) FROM posts            │
│    ├─> Tổng số comments: SELECT COUNT(*) FROM comments         │
│    └─> Tổng số likes:    SELECT COUNT(*) FROM likes            │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 5. THỐNG KÊ HOẠT ĐỘNG CÁ NHÂN                                   │
│    ├─> Số bài viết của tôi                                     │
│    ├─> Số bình luận của tôi                                    │
│    ├─> Số lượt tôi đã like                                     │
│    └─> Số lượt like nhận được từ người khác                    │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 6. HIỂN THỊ GIAO DIỆN                                           │
│    ├─> Stats cards: Thống kê hệ thống (Admin)                  │
│    ├─> Stats cards: Thống kê cá nhân                           │
│    │                                                            │
│    ├─> BIỂU ĐỒ XU HƯỚNG CỘNG ĐỒNG (Chart.js - Doughnut)        │
│    │   └─> Hiển thị top tags phổ biến nhất                     │
│    │                                                            │
│    └─> BIỂU ĐỒ XU HƯỚNG CÁ NHÂN (Chart.js - Doughnut)          │
│        └─> Hiển thị sở thích user dựa trên score               │
└─────────────────────────────────────────────────────────────────┘
```

#### Cơ chế theo dõi sở thích (User Interests):

```
┌─────────────────────────────────────────────────────────────────┐
│ HÀM trackInterests($pdo, $user_id, $tags_string)                │
│                                                                 │
│ Được gọi khi user XEM một bài viết (trong post.php)            │
│                                                                 │
│ LUỒNG XỬ LÝ:                                                    │
│ 1. Tách tags từ bài viết (explode by ",")                      │
│ 2. Với mỗi tag:                                                 │
│    INSERT INTO user_interests (user_id, tag, score)            │
│    VALUES (?, ?, 1)                                            │
│    ON DUPLICATE KEY UPDATE score = score + 1                   │
│                                                                 │
│ → Mỗi lần xem bài có tag X, score của tag X tăng 1             │
│ → Tags có score cao = User quan tâm nhiều                      │
└─────────────────────────────────────────────────────────────────┘
```

---

### 5. ✏️ TẠO BÀI VIẾT MỚI

**File:** `pages/create_post.php`

#### Luồng xử lý:

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. KIỂM TRA ĐĂNG NHẬP                                           │
│    └─> Nếu chưa đăng nhập → Redirect về login.php              │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 2. HIỂN THỊ FORM TẠO BÀI VIẾT                                   │
│    ├─> Tiêu đề (5-200 ký tự)                                   │
│    ├─> Nội dung (10-5000 ký tự)                                │
│    ├─> Tags (tối đa 5 tag, 100 ký tự)                          │
│    ├─> Quyền riêng tư (public/private)                         │
│    └─> File đính kèm (ảnh, PDF, Word, Excel - max 100MB)       │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 3. NGƯỜI DÙNG SUBMIT (POST)                                     │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 4. VALIDATE DỮ LIỆU                                             │
│    ├─> Kiểm tra tiêu đề:  không rỗng, 5-200 ký tự               │
│    ├─> Kiểm tra nội dung: không rỗng, 10-5000 ký tự            │
│    ├─> Xử lý tags: tách bằng dấu "," hoặc khoảng trắng         │
│    │   → Thêm "#" nếu chưa có                                  │
│    │   → Tối đa 5 tags                                         │
│    ├─> Kiểm tra privacy: public hoặc private                   │
│    └─> Kiểm tra files:                                          │
│        ├─> Loại file cho phép: image, pdf, doc, xls            │
│        └─> Kích thước <= 100MB                                 │
│                                                                 │
│    [LỖI] ──> Hiển thị thông báo lỗi                            │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 5. LƯU BÀI VIẾT (TRANSACTION)                                   │
│    ├─> BEGIN TRANSACTION                                       │
│    │                                                            │
│    ├─> INSERT INTO posts (user_id, title, content,             │
│    │      tags, privacy, status='unsolved')                    │
│    │                                                            │
│    ├─> Lấy post_id = lastInsertId()                            │
│    │                                                            │
│    ├─> Upload từng file đính kèm:                               │
│    │   ├─> handleUpload($file) → Lưu vào uploads/              │
│    │   └─> INSERT INTO attachments (post_id, file_path,        │
│    │          file_type)                                       │
│    │                                                            │
│    └─> COMMIT                                                  │
│                                                                 │
│    [LỖI] ──> ROLLBACK, hiển thị lỗi                            │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 6. REDIRECT                                                     │
│    └─> Hiển thị "Thành công!", chuyển về index.php             │
└─────────────────────────────────────────────────────────────────┘
```

---

### 6. 📖 XEM CHI TIẾT BÀI VIẾT

**File:** `pages/post. php`

#### Luồng xử lý:

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. LẤY THÔNG TIN BÀI VIẾT                                       │
│    ├─> Lấy post_id từ URL:  $_GET['id']                         │
│    ├─> Truy vấn:  getPost($pdo, $post_id)                       │
│    │   → SELECT p.*, u.ho_ten, u. username, u.account_level     │
│    │     FROM posts p JOIN user u WHERE p.id = ?                │
│    │                                                            │
│    └─> Kiểm tra bài viết tồn tại, nếu không → die()            │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 2. KIỂM TRA QUYỀN XEM                                           │
│    └─> Nếu bài viết private:                                    │
│        ├─> Chỉ chủ bài viết hoặc Admin được xem                │
│        └─> Nếu không có quyền → die()                          │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 3. GHI NHẬN LƯỢT XEM (nếu đăng nhập)                            │
│    ├─> incrementViews($pdo, $post_id)                          │
│    │   → UPDATE posts SET views = views + 1                    │
│    │                                                            │
│    └─> trackInterests($pdo, user_id, tags)                     │
│        → INSERT/UPDATE user_interests (user_id, tag, score+1)  │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 4. XỬ LÝ CÁC ACTION (POST REQUEST)                              │
│    │                                                            │
│    ├─> LIKE: INSERT INTO likes (user_id, target_id, 'post')    │
│    ├─> UNLIKE: DELETE FROM likes WHERE ...                      │
│    │                                                            │
│    ├─> THÊM BÌNH LUẬN:                                          │
│    │   └─> INSERT INTO comments (post_id, user_id, content,    │
│    │          parent_id)                                       │
│    │                                                            │
│    ├─> XÓA BÌNH LUẬN (chủ comment/chủ bài/admin):              │
│    │   └─> DELETE FROM comments WHERE id=?  OR parent_id=?      │
│    │                                                            │
│    ├─> SỬA BÌNH LUẬN (chỉ chủ comment):                        │
│    │   └─> UPDATE comments SET content=?  WHERE id=?            │
│    │                                                            │
│    ├─> SỬA BÀI VIẾT (chỉ chủ bài):                             │
│    │   └─> UPDATE posts SET title=?, content=?, tags=?          │
│    │                                                            │
│    ├─> XÓA BÀI VIẾT (chủ bài hoặc admin):                      │
│    │   └─> DELETE FROM posts WHERE id=?                        │
│    │                                                            │
│    └─> TOGGLE TRẠNG THÁI (solved/unsolved):                    │
│        └─> UPDATE posts SET status=?                           │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 5. LẤY DỮ LIỆU HIỂN THỊ                                         │
│    ├─> getAttachments($pdo, $post_id) → Lấy file đính kèm      │
│    ├─> getCommentsSorted() → Lấy comments (có sắp xếp)         │
│    ├─> getLikeCount() → Đếm số like                            │
│    └─> hasLiked() → Kiểm tra user đã like chưa                 │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 6. HIỂN THỊ GIAO DIỆN                                           │
│    ├─> Header: Avatar, tên tác giả, thời gian, privacy, status │
│    ├─> Tiêu đề + Tags + Nội dung                               │
│    ├─> Ảnh đính kèm (click để phóng to)                        │
│    ├─> File đính kèm (link download)                           │
│    ├─> Interaction bar:  Like, Comment                          │
│    ├─> Form bình luận (nếu đăng nhập)                          │
│    ├─> Danh sách bình luận (nested - có reply)                 │
│    └─> Modal sửa bài viết (nếu là chủ bài)                     │
└─────────────────────────────────────────────────────────────────┘
```

---

### 7. 👤 TRANG HỒ SƠ CÁ NHÂN

**File:** `pages/profile.php`

#### Luồng xử lý:

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. LẤY THÔNG TIN NGƯỜI DÙNG                                     │
│    ├─> Lấy username từ URL: $_GET['username']                  │
│    ├─> Query: SELECT * FROM user WHERE username = ?            │
│    └─> Nếu không tìm thấy → die()                              │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 2. XỬ LÝ ACTION LIKE (POST)                                     │
│    ├─> Kiểm tra đăng nhập                                      │
│    ├─> Toggle like:  Kiểm tra → INSERT hoặc DELETE              │
│    └─> Redirect về profile                                     │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 3. LẤY BÀI VIẾT CÔNG KHAI CỦA USER                              │
│    └─> SELECT p.*, (COUNT comments), (COUNT likes)             │
│        FROM posts p WHERE user_id = ?  AND privacy = 'public'   │
│        ORDER BY created_at DESC                                │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 4. HIỂN THỊ GIAO DIỆN                                           │
│    ├─> Profile header: Avatar (chữ cái đầu), tên, username     │
│    ├─> Vai trò: Admin / Giảng viên / Sinh viên                 │
│    └─> Grid các bài viết công khai với:                        │
│        ├─> Preview ảnh/file                                    │
│        └─> Buttons:  Like, Comment                              │
└─────────────────────────────────────────────────────────────────┘
```

---

### 8. 📚 KHO TÀI LIỆU HỌC TẬP

**File:** `pages/resources.php`

#### Luồng xử lý:

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. XỬ LÝ XÓA TÀI LIỆU (POST)                                    │
│    ├─> Kiểm tra đăng nhập                                      │
│    ├─> Kiểm tra quyền:  Chủ tài liệu hoặc Admin                 │
│    ├─> Xóa file vật lý:  unlink(file_path)                      │
│    └─> DELETE FROM tai_lieu WHERE id_tai_lieu = ?               │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 2. XỬ LÝ UPLOAD TÀI LIỆU (POST)                                 │
│    ├─> Kiểm tra đăng nhập                                      │
│    ├─> Lấy thông tin:  title, description, category, topic      │
│    ├─> Validate file:                                           │
│    │   ├─> Kiểm tra lỗi upload                                 │
│    │   └─> Kiểm tra size <= 100MB                              │
│    ├─> Upload file:                                             │
│    │   ├─> Tạo filename: time() + tên file gốc                 │
│    │   └─> move_uploaded_file → uploads/resources/             │
│    └─> Lưu vào DB:                                              │
│        └─> INSERT INTO tai_lieu (id_danh_muc, id_user,         │
│               tieu_de, mo_ta, duong_dan_file, ngay_upload)     │
│            * mo_ta format: [CATEGORY: xxx][TOPIC:xxx] mô tả     │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 3. TÌM KIẾM VÀ LỌC TÀI LIỆU                                     │
│    ├─> Tìm kiếm theo:  tieu_de, mo_ta, username                 │
│    ├─> Lọc theo category: [CATEGORY:xxx] trong mo_ta           │
│    ├─> Lọc theo topic:  [TOPIC:xxx] trong mo_ta                 │
│    └─> Sắp xếp: newest / oldest                                │
│                                                                 │
│    SELECT tl.*, u.username, u.ho_ten                           │
│    FROM tai_lieu tl JOIN user u                                │
│    WHERE (search conditions)                                    │
│    ORDER BY ngay_upload                                        │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 4. THỐNG KÊ                                                     │
│    ├─> Đếm tổng số tài liệu                                    │
│    ├─> Đếm theo loại file (PDF, DOC, XLS...)                   │
│    └─> Danh sách categories và topics                          │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 5. HIỂN THỊ GIAO DIỆN                                           │
│    ├─> Stats bar:  Tổng số, theo loại file                      │
│    ├─> Filter section: Search, Category, Topic, Sort           │
│    ├─> Form upload (nếu đăng nhập)                             │
│    └─> Grid tài liệu với:                                      │
│        ├─> Icon theo loại file (màu sắc khác nhau)             │
│        ├─> Tên, category, topic, mô tả                         │
│        ├─> Người upload, ngày upload                           │
│        └─> Buttons: Download, Delete (nếu có quyền)            │
└─────────────────────────────────────────────────────────────────┘
```

---

### 9. 👥 QUẢN LÝ NGƯỜI DÙNG (ADMIN)

**File:** `pages/admin_users.php`

#### Luồng xử lý:

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. KIỂM TRA QUYỀN ADMIN                                         │
│    └─> Nếu account_level != 0 → Redirect về index.php          │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 2. XỬ LÝ XÓA USER (GET:  ? action=delete&id=...)                  │
│    ├─> Không cho xóa chính mình                                │
│    └─> DELETE FROM user WHERE id_user = ?                      │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 3. XỬ LÝ THAY ĐỔI VAI TRÒ (POST:  action=change_role)            │
│    ├─> Không cho đổi vai trò chính mình                        │
│    ├─> Validate:  new_role ∈ {0, 1, 2}                          │
│    └─> UPDATE user SET account_level = ? WHERE id_user = ?      │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 4. XỬ LÝ TẠO USER MỚI (POST: action=create_user)                │
│    ├─> Validate:                                                │
│    │   ├─> username >= 3 ký tự                                 │
│    │   ├─> ho_ten không rỗng                                   │
│    │   ├─> password >= 6 ký tự                                 │
│    │   └─> account_level ∈ {0, 1, 2}                          │
│    ├─> Kiểm tra username chưa tồn tại                          │
│    └─> INSERT INTO user (ho_ten, username, password,           │
│           account_level) VALUES (?, ?, ?, ?)                   │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 5. TÌM KIẾM VÀ LỌC USER                                         │
│    ├─> Tìm theo: username, ho_ten                              │
│    └─> Lọc theo vai trò: 0/1/2                                 │
│                                                                 │
│    SELECT id_user, ho_ten, username, account_level             │
│    FROM user WHERE (conditions) ORDER BY id_user               │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 6. THỐNG KÊ                                                     │
│    ├─> Tổng số users                                           │
│    ├─> Số Admin (account_level = 0)                            │
│    ├─> Số Giảng viên (account_level = 1)                       │
│    └─> Số Sinh viên (account_level = 2)                        │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 7. HIỂN THỊ GIAO DIỆN                                           │
│    ├─> Form tạo user mới (trái)                                │
│    ├─> Stats cards: Tổng, Admin, Giảng viên, Sinh viên         │
│    ├─> Search + Filter by role                                 │
│    └─> Bảng users với:                                         │
│        ├─> ID, Username, Họ tên, Vai trò (badge)               │
│        └─> Actions:  Dropdown đổi role, Xóa                     │
│            * Không hiện actions cho chính mình                 │
└─────────────────────────────────────────────────────────────────┘
```

---

### 10. 🔥 BÀI VIẾT TRENDING

**Hàm:** `getTrending()` trong `includes/functions.php`

#### Luồng xử lý:

```
┌─────────────────────────────────────────────────────────────────┐
│ ĐIỀU KIỆN ĐỂ BÀI VIẾT ĐƯỢC COI LÀ TRENDING                      │
│                                                                 │
│ 1. Được đăng trong 7 ngày gần nhất                             │
│    → created_at >= DATE_SUB(NOW(), INTERVAL 7 DAY)             │
│                                                                 │
│ 2. Là bài viết công khai                                       │
│    → privacy = 'public'                                        │
│                                                                 │
│ 3. Có >= 5 lượt like VÀ >= 5 bình luận                         │
│    → HAVING like_count >= 5 AND comment_count >= 5             │
│                                                                 │
│ 4. TÍNH ĐIỂM ENGAGEMENT:                                        │
│    engagement_score = (số like × 2) + (số comment × 3)         │
│                                                                 │
│ 5. SẮP XẾP:                                                      │
│    → ORDER BY engagement_score DESC, created_at DESC           │
└─────────────────────────────────────────────────────────────────┘

QUERY:
SELECT p.*, u.ho_ten, u.username,
       COUNT(DISTINCT l.id) as like_count,
       COUNT(DISTINCT c.id) as comment_count,
       (COUNT(DISTINCT l.id) * 2 + COUNT(DISTINCT c.id) * 3) as engagement_score
FROM posts p
JOIN user u ON p.user_id = u. id_user
LEFT JOIN likes l ON l.target_id = p. id AND l.target_type = 'post'
LEFT JOIN comments c ON c. post_id = p. id
WHERE p.created_at >= DATE_SUB(NOW(), INTERVAL 7 DAY)
      AND p.privacy = 'public'
GROUP BY p.id
HAVING (like_count >= 5 AND comment_count >= 5)
ORDER BY engagement_score DESC, p.created_at DESC
LIMIT 5
```

---

### 11. 💾 HÀM UPLOAD FILE

**Hàm:** `handleUpload()` trong `includes/functions.php`

#### Luồng xử lý:

```
┌─────────────────────────────────────────────────────────────────┐
│ handleUpload($file)                                             │
│                                                                 │
│ Input:  $_FILES['... '] array                                    │
│ Output: ['success' => bool, 'path' => string, 'type' => string]│
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 1. KIỂM TRA UPLOAD_DIR                                          │
│    └─> Nếu chưa define → Return error                          │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 2. KIỂM TRA LỖI UPLOAD                                          │
│    └─> $file['error'] !== UPLOAD_ERR_OK → Return error         │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 3. KIỂM TRA KÍCH THƯỚC                                          │
│    └─> $file['size'] > 100MB → Return error                    │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 4. TẠO TÊN FILE MỚI                                             │
│    ├─> Lấy extension từ file gốc                               │
│    └─> filename = uniqid() + '_' + time() + '.' + extension    │
│        Ví dụ: 65f3a1b2c_1704067200.pdf                         │
└────────────────────────────────────────────────────────────────┬┘
                                                                 │
┌────────────────────────────────────────────────────────────────▼┐
│ 5. DI CHUYỂN FILE                                               │
│    └─> move_uploaded_file($tmp_name, UPLOAD_DIR + filename)    │
│                                                                 │
│    [THÀNH CÔNG] → Return {success: true, path, type}           │
│    [THẤT BẠI]   → Return {success:  false, error}               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔒 Bảo mật

-   **XSS Prevention**: Sử dụng hàm `h()` (htmlspecialchars) cho tất cả output
-   **SQL Injection Prevention**: Sử dụng Prepared Statements với PDO
-   **Session Management**: Kiểm tra session cho các trang yêu cầu đăng nhập
-   **Access Control**: Kiểm tra quyền trước khi thực hiện các action (xóa, sửa...)

---

## 🎨 Giao diện

-   **Responsive Design**: Tương thích với mọi thiết bị
-   **Modern UI**: Gradient colors, rounded corners, shadows
-   **Icon Library**: BoxIcons
-   **Font**: Poppins, Plus Jakarta Sans

---

## 👨‍💻 Tác giả

**Chu Quang Duy** - K73 - Learning2ne1 Team

---

## 📄 License

MIT License
